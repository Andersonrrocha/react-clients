import React from 'react'
// import logo from '../img/ze.png'

const Header = () => {

    return (
        <header className="center">
            {/* <img src={logo} alt='' onClick={() => {history.push("/")}}/> */}
        </header>
    )
}

export default Header

